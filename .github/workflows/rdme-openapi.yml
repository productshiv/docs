# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2024-01-02T13:48:57.826Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Blockchain Data
        env:
          README_API_KEY: ${{ secrets.README_API_KEY }}
        uses: readmeio/rdme@v8
        with:
          rdme: openapi blockchain_data/openapi.yml --key=${{ secrets.README_API_KEY }} --id=65b89aca5d7811001010ed75

      - name: Notifications
        env:
          README_API_KEY: ${{ secrets.README_API_KEY }}
        uses: readmeio/rdme@v8
        with:
          rdme: openapi notifications/openapi.yml --key=${{ secrets.README_API_KEY }} --id=65b89afa7868820016d7f652

      - name: Blockhain Utility
        env:
          README_API_KEY: ${{ secrets.README_API_KEY }}
        uses: readmeio/rdme@v8
        with:
          rdme: openapi blockchain_utility/openapi.yml --key=${{ secrets.README_API_KEY }} --id=65b89df5360e780022ac7d53

      - name: RPC
        env:
          README_API_KEY: ${{ secrets.README_API_KEY }}
        uses: readmeio/rdme@v8
        with:
          rdme: openapi rpc/openapi.yml --key=${{ secrets.README_API_KEY }} --id=65b89f2c763bfe002336c7c2
