# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2024-01-02T13:48:57.826Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Data API
        env:
          README_API_KEY: ${{ secrets.README_API_KEY }}
        uses: readmeio/rdme@v8
        with:
          rdme: openapi data_api/openapi.yml --key=${{ secrets.README_API_KEY }} --id=6594141cd3dc9c00600a98c5

      - name: Notifications
        env:
          README_API_KEY: ${{ secrets.README_API_KEY }}
        uses: readmeio/rdme@v8
        with:
          rdme: openapi notifications/openapi.yml --key=${{ secrets.README_API_KEY }} --id=6596cf7a1065e5000fe12649

      - name: RPC
        env:
          README_API_KEY: ${{ secrets.README_API_KEY }}
        uses: readmeio/rdme@v8
        with:
          rdme: openapi rpc/openapi.yml --key=${{ secrets.README_API_KEY }} --id=65982f7b8d81bf00695175d3
