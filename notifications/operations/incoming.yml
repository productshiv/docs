openapi: 3.1.0
info:
  title: Incoming Wallet Transfers
  version: "1.0.0"
paths:
  create:
    post:
      summary: create subscriptions
      description: Creates subscriptions.
      tags: ["Incoming Wallet Transfers"]
      responses:
        200:
          $ref: "../../components/responses.yml#/components/OK"
        400:
          $ref: "../../components/responses.yml#/components/Error400"
      operationId: sub_create
